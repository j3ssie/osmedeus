name: flow-with-params
desc: run normal routine
type: sample

params:
  - firstTimeout: '5s'

routines:
  - modules:
      - timeout-module
      - with-threadshold

  - modules:
      - loop-step
      # - parallel
