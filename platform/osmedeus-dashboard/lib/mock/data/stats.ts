import type { DashboardStats } from "@/lib/types/asset";

export const mockStats: DashboardStats = {
  workflows: 12,
  workspaces: 24,
  subdomains: 15847,
  httpAssets: 8234,
  vulnerabilities: 127,
};
