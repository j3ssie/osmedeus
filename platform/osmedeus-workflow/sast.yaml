kind: flow
name: sast
description: Analyze a single URL by fingerprinting, crawling, scanning, and discovering
tags: recon, sast, standard
help:
  example_targets: ['https://github.com/user/repo', 'https://github.com/juice-shop/juice-shop/archive/refs/heads/master.zip']
  usage: osmedeus run -f sast -t https://github.com/user/repo

dependencies:
  variables:
    - name: Target
      type: string
      required: true

modules:
  - name: do-scan-repo
    path: fragments/do-scan-repo.yaml
