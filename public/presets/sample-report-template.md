# Security Report for {{Workspace}}

**Generated**: {{TaskDate}}
**Target**: {{Target}}
**Run UUID**: {{RunUUID}}

---

## Summary Statistics

```osm-func
"Total subdomains: " + db_total_subdomains()
```

```osm-func
"Total URLs: " + db_total_urls()
```

```osm-func
"Total assets: " + db_total_assets()
```

```osm-func
"Total vulnerabilities: " + db_total_vulns()
```

---

## Vulnerability Breakdown by Severity

| Severity | Count                           |
|----------|---------------------------------|
| Critical | `db_vuln_critical()`{.osm-func} |
| High     | `db_vuln_high()`{.osm-func}     |
| Medium   | `db_vuln_medium()`{.osm-func}   |
| Low      | `db_vuln_low()`{.osm-func}      |

---

## All Vulnerabilities

```osm-func
db_select_vulnerabilities("{{Workspace}}", "markdown")
```

---

## All Assets

```osm-func
db_select_assets("{{Workspace}}", "markdown")
```

---

## Custom SQL Query Example

The following demonstrates using a raw SQL query to get vulnerability counts grouped by severity:

```osm-func
db_select("SELECT severity, COUNT(*) as count FROM vulnerabilities GROUP BY severity ORDER BY count DESC", "markdown")
```

---

## String Function Examples

**Trimmed string:**
```osm-func
trim("  Example trimmed text  ")
```

**Uppercase:**
```osm-func
toUpperCase("example")
```

**String concatenation:**
```osm-func
"Workspace: " + "{{Workspace}}" + " | Target: " + "{{Target}}"
```

---

*Report generated by Osmedeus*
